import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    textTotal: {
        fontSize: 24,
        color: '#FFFFFF',
        fontWeight: 'bold',
        textAlign: 'center'
    },
    textObject: {
        fontSize: 16,
        color: '#FFFFFF',
        fontWeight: 'bold',
        textAlign: 'center'
    }
})